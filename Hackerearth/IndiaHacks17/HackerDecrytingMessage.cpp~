#include <bits/stdc++.h>

using namespace std;

int main()
{
  int len, q;
  cin>>len>>q;

  vector<int> arr;
  for(int i = 0; i < len; i++)
    {
      int no;
      cin>>no;
      arr.push_back(no);
    }
  return 0;
}
