#include <bits/stdc++.h>

using namespace std;

int main()
{
  int len;
  cin>>len;

  bitset<100> arr;
  for(int i = 0; i < len; i++)
    {
      int no;
      cin>>no;
      arr[i] = no;
    }
  cout<<arr<<endl;
  return 0;
}
