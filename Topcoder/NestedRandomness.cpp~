#include <iostream>

using namespace std;

int main()
{
  double range, nesting, target;
  cin>>range>>nesting>>target;

  double value;

  ll start = target + nesting -1;
  for(i = start; i < range; i++)
    {
      
    }
    
  return 0;
}
